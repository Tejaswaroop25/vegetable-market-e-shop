# MongoDB Connection URI
# Replace with your own connection string (e.g., from MongoDB Atlas)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/vegetable-market?retryWrites=true&w=majority

# Port for the server (default is 5000)
PORT=5000
